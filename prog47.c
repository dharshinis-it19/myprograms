#include<stdio.h>
int main()
{
 int l,b,a;
 printf("enter the length of the rectangle");
 scanf("%d",&l);
 printf("enter the breadth of the rectangle");
 scanf("%d",&b);
 a=l*b;
 printf("the area of the rectangle is %d units",a);
 return 0;
}

